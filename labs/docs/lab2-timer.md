# Lab 2: PIT And Timer IRQs

Goal: program the virtual i8254 PIT, subscribe to IRQ0, and use
`lcom_event_wait` to react to timer ticks.

Reference:

```sh
build/lcom run --headless -- build/examples/timer_int 3
```
