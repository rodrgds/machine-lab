# Lab 5: VBE Framebuffer And XPM

Goal: set a virtual VBE mode, map the framebuffer, draw pixels/rectangles, load
small XPM sprites, and present/dump frames.

Reference:

```sh
build/lcom run --headless --dump-frame build/rectangle.ppm -- build/examples/vbe_rectangle
```
